spring.application.name=BillWise

# -----------------------------------------------------------
# -----------------------------------------------------------
# POSTGRESQL CONNECTION & JPA/HIBERNATE CONFIGURATION
# -----------------------------------------------------------
# Update these with your local PostgreSQL credentials
spring.datasource.url=jdbc:postgresql://localhost:5432/billwise_db
spring.datasource.username=postgres
spring.datasource.password=Aman@15112003

# Hibernate will automatically create/update tables based on your entities
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Best Practice: Disable Open-in-View to manage sessions explicitly in the service layer
spring.jpa.open-in-view=false

# -----------------------------------------------------------
# JWT SECURITY CONFIGURATION (CRITICAL)
# -----------------------------------------------------------
# !!! UPDATE THESE VALUES !!!
# A long, random base64 encoded string (at least 256 bits or 32 characters)
jwt.secret=YourBase64EncodedSecretKeyHere1234567890abcd
# Token expiration time in milliseconds (e.g., 24 hours)
jwt.expiration=86400000 

# -----------------------------------------------------------
# SERVER & LOGGING
# -----------------------------------------------------------
server.port=8080
logging.level.org.springframework.security=INFO
logging.level.com.aksps.billwise=DEBUG
